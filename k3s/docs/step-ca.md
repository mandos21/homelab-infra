# Dedicated Step-CA For k3s

The edge host automation was moved out of `k3s/` and into `edge/`.

Use:
- `edge/docs/step-ca.md`
- `edge/ansible/inventory/hosts.yaml`
- `edge/ansible/playbooks/step-ca-bootstrap.yaml`
