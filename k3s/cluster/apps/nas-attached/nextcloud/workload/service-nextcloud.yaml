apiVersion: v1
kind: Service
metadata:
  name: nextcloud
  namespace: nextcloud
  labels:
    app.kubernetes.io/part-of: nextcloud
    app.kubernetes.io/name: nextcloud
    app.kubernetes.io/component: app
spec:
  selector:
    app.kubernetes.io/name: nextcloud
    app.kubernetes.io/component: app
  ports:
    - name: https
      port: 443
      targetPort: https
