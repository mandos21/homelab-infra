apiVersion: v1
kind: Service
metadata:
  name: navidrome
  namespace: navidrome
spec:
  selector:
    app.kubernetes.io/name: navidrome
    app.kubernetes.io/component: app
  ports:
    - name: http
      port: 80
      targetPort: http
