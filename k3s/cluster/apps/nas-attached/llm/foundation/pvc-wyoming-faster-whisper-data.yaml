apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: wyoming-faster-whisper-data
  namespace: llm
  annotations:
    kustomize.toolkit.fluxcd.io/prune: disabled
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 20Gi
  storageClassName: ""
  volumeName: wyoming-faster-whisper-data
