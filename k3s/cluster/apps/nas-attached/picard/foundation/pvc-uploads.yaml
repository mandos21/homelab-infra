apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: picard-uploads
  namespace: picard
  annotations:
    kustomize.toolkit.fluxcd.io/prune: disabled
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Ti
  storageClassName: ""
  volumeName: picard-uploads
