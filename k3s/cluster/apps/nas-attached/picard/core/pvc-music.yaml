apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: picard-music
  namespace: picard
  annotations:
    kustomize.toolkit.fluxcd.io/prune: disabled
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 4Ti
  storageClassName: ""
  volumeName: picard-music
