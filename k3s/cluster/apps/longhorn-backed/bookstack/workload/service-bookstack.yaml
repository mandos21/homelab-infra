apiVersion: v1
kind: Service
metadata:
  name: bookstack
  namespace: bookstack
  labels:
    app.kubernetes.io/name: bookstack
    app.kubernetes.io/component: app
spec:
  selector:
    app.kubernetes.io/name: bookstack
    app.kubernetes.io/component: app
  ports:
    - name: http
      port: 80
      targetPort: http