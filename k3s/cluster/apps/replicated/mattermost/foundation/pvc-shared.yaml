apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mattermost-shared
  namespace: mattermost
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 200Gi
  volumeName: mattermost-shared
  storageClassName: ""
