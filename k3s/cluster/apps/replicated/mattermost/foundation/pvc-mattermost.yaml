apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mattermost
  namespace: mattermost
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
  storageClassName: longhorn-retain