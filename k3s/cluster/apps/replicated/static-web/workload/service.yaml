apiVersion: v1
kind: Service
metadata:
  name: static-web
  namespace: static-web
  labels:
    app.kubernetes.io/name: static-web
    app.kubernetes.io/component: app
spec:
  selector:
    app.kubernetes.io/name: static-web
    app.kubernetes.io/component: app
  ports:
    - name: http
      port: 8080
      targetPort: http
