apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: homelab-infra
  namespace: flux-system
spec:
  interval: 1m0s
  url: ssh://git@github.com/mandos21/homelab-infra
  ref:
    branch: main
  secretRef:
    name: flux-system
