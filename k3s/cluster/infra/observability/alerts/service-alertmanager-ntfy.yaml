apiVersion: v1
kind: Service
metadata:
  name: alertmanager-ntfy
  namespace: observability
spec:
  selector:
    app.kubernetes.io/name: alertmanager-ntfy
  ports:
    - name: http
      port: 8000
      targetPort: http
