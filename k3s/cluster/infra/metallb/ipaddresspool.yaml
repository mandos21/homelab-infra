apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
  name: ingress-pool
  namespace: metallb-system
spec:
  # Replace this starter range with a DHCP-excluded block on the mgmt LAN.
  addresses:
    - 192.168.1.240-192.168.1.242
  autoAssign: true
