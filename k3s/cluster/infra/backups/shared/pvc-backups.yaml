apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: backups-nfs
  namespace: backups
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Ti
  storageClassName: ""
  volumeName: backups-nfs
